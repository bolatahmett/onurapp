export enum TruckStatus {
  ACTIVE = 'ACTIVE',
  CLOSED = 'CLOSED',
}

export enum UnitType {
  PALLET = 'PALLET',
  CRATE = 'CRATE',
}

export enum InvoiceStatus {
  DRAFT = 'DRAFT',
  ISSUED = 'ISSUED',
  PAID = 'PAID',
  CANCELLED = 'CANCELLED',
}

export enum BackupType {
  MANUAL = 'MANUAL',
  AUTOMATIC = 'AUTOMATIC',
}
